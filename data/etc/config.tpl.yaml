# 此文件为模板文件
# 将此文件存放至 data/etc/config.yaml

# 模型接口配置
modelApi:
  url: "https://open.bigmodel.cn/api/paas/v4/chat/completions"
  model: "glm-4.7-flash"
  apiKey: <string>
  thinking: "disabled"
  stream: false
  maxTokens: 65536
  temp: 1.0

# 角色对话配置
character:

  # 对话记忆轮数
  memory: 6

# 文本编辑器
textEditor: "vi"